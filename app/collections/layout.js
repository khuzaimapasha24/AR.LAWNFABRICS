export const metadata = {
  title: 'Collections | AR.LAWN',
  description: 'Explore the finest luxury fabrics curated for every occasion.',
};

export default function CollectionsLayout({ children }) {
  return children;
}
